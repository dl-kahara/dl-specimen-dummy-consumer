apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dummy-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "dummy-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dummy-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
